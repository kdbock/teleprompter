# Progress Tracker - May 25, 2026

## Scope Reviewed
- `lib/features/prompter/screens/record_with_prompter_screen.dart`
- `lib/features/prompter/screens/recordings_screen.dart`
- `lib/shared/services/recording_service.dart`
- `lib/features/prompter/screens/prompter_screen.dart`

## Verified Current State (Code, not docs)

### Completed
- Record with prompter exists and is usable (countdown, start/stop, timer).
- Tap-to-pause/resume scrolling during recording exists.
- Focus mask + adjustable read line exists.
- Mirror mode exists.
- Front/rear camera toggle exists.
- Resolution selector exists (1080p/4K attempt via camera presets).
- FPS selector exists (30/60 with device capability fallback).
- Hands-free controls exist (hardware volume-key trigger toggle).
- Audio preflight checks exist (camera/mic/storage readiness + ambient sound-level meter).
- Orientation lock + touch lock exist.
- Script markers / jump points exist.
- Section loop for retakes exists (loop start/end with auto-wrap during scroll).
- Persisted in-session recorder settings (font, scroll speed, read-line) per script.
- Auto-resume from last script position exists (restores scroll offset per script).
- Local save pipeline exists with metadata persistence (`Hive`).
- Crash/reload recovery for interrupted finalize exists (`recordings_recovery`).
- Recordings list + playback exist.
- Quick trim exists in playback flow (in-app trim range metadata + bounded playback).
- Rename recording exists.
- Tags editing exists.
- Best-take toggle exists.
- Review overlay controls are partially implemented in playback:
  - Lower third text + position + basic color controls
  - Captions mode toggle (word/line), style controls, manual/script/audio generation path

### Partially Implemented / Needs Hardening
- Review v2 overlays now support styled export snapshots as derived takes (metadata profile), but no burned-in video render yet.
- Caption generation depends on transcription path and fallback; quality/error states need refinement.
- Gallery save is best-effort and should keep edge-case QA coverage.

### Not Yet Implemented
- Burned-in/exported review overlays (current overlays are preview-time only).

## Today’s Priority Plan
1. Implement burned-in overlay render export (video compositing).
2. Caption generation quality/error-state hardening.
3. Gallery save edge-case QA pass.
4. Green screen (deferred).

## Work Started Today
- [x] Code review + status reconciliation completed.
- [x] Speed presets in record-with-prompter.
- [x] Script markers/jump support.
- [x] Persist in-session settings.
- [x] Camera toggle + resolution selector.
- [x] Auto-resume from last script position.
- [x] Quick trim in playback flow.
- [x] FPS selector (30/60) with fallback handling.
- [x] Hands-free controls (hardware key toggle).
- [x] Audio preflight + ambient sound-level checks.
- [x] Section loop for retakes.
- [x] Styled export snapshot path (derived take metadata export).

## Notes
- `docs/BUILD_TIMELINE.md` under-reports implemented items compared to code (tags/best-take and partial Review v2 controls are already present).
- `docs/IMPLEMENTATION_GUIDE.md` is historical/scaffold-level and should not be treated as live status.
